import { Component, inject, OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { SupabaseService } from '../../services/supabase.service'; @Component({ selector: 'app-home', standalone: true, imports: [CommonModule, FormsModule], template: ` <div class="home-container"> <div class="logo-container"> <img src="assets/foodmoney512.png" alt="FoodMoney Logo" class="logo" /> </div> <h1 class="home-title">FoodMoney</h1> <p class="home-description">Gestisci le tue spese quotidiane</p> <button class="create-btn" (click)="openOrderPopup()"> + Crea Ordine </button> </div> `, styles: [ `.home-container { display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 2rem; min-height: calc(100vh - 80px); background: #fff7ed; } .logo-container { margin-bottom: 1.5rem; } .logo { width: 250px; height: auto; border-radius: 1rem; box-shadow: 0 10px 15px -3px rgba(249, 115, 22, 0.2); } .home-title { color: #9a3412; font-size: 2.5rem; font-weight: 800; margin: 0.5rem 0; text-align: center; } .home-description { color: #9a3412; opacity: 0.7; font-size: 1rem; text-align: center; margin-bottom: 2rem; } .create-btn { padding: 1rem 2rem; background: #f97316; color: white; border: none; border-radius: 1rem; font-weight: 700; font-size: 1rem; cursor: pointer; transition: all 0.2s; } .create-btn:hover { background: #ea580c; transform: translateY(-2px); box-shadow: 0 10px 15px -3px rgba(249, 115, 22, 0.3); } @media (max-width: 768px) { .home-container { padding: 1.5rem; } .logo { width: 200px; } .home-title { font-size: 2rem; } .home-description { font-size: 0.9rem; } } `] }) export class HomeComponent implements OnInit { isOrderPopupOpen = false; orderForm = { restaurantId: '', data: '', importo: 0 }; supabase = inject(SupabaseService); restaurants: any[] = []; ngOnInit() { this.loadRestaurants(); } async loadRestaurants() { try { const { data } = await this.supabase.getRestaurants(); this.restaurants = data || []; } catch (error) { console.error('Errore caricamento ristoranti:', error); } } openOrderPopup() { this.isOrderPopupOpen = true; this.orderForm = { restaurantId: '', data: new Date().toISOString().split('T')[0], importo: 0 }; } closeOrderPopup() { this.isOrderPopupOpen = false; } async saveOrder() { if (!this.orderForm.restaurantId || !this.orderForm.data || !this.orderForm.importo) { return; } const selectedRestaurant = this.restaurants.find(r => r.id === this.orderForm.restaurantId); if (!selectedRestaurant) return; try { const { error } = await this.supabase.addExpense( parseFloat(this.orderForm.importo.toString()), selectedRestaurant.name, 'Ristorante', this.orderForm.data, selectedRestaurant.color ); if (error) { console.error('Errore nel salvataggio ordine:', error); return; } this.closeOrderPopup(); this.loadOrders(); } catch (error) { console.error('Errore nel salvataggio ordine:', error); } } }